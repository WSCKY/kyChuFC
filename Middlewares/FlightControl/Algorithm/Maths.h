#ifndef __MATHS_H
#define __MATHS_H

#include "stm32f7xx.h"

#include <math.h>

/* Exported macros -----------------------------------------------------------*/
#define ABS(x)                                   ((x) >= 0 ? (x) : -(x))
#define SQUARE(x)                                ((x) * (x))

#endif /* __MATHS_H */

/* ------------------------ (C) COPYRIGHT kyChu ----------- END OF FILE ----- */
